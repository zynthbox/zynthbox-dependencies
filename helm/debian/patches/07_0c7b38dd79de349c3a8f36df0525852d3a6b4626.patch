From 0c7b38dd79de349c3a8f36df0525852d3a6b4626 Mon Sep 17 00:00:00 2001
From: Warren Falk <warren@warrenfalk.com>
Date: Sat, 23 Dec 2017 15:30:22 -0500
Subject: [PATCH] List only readable midi ports in the linux input devices list

---
 JUCE/modules/juce_audio_devices/native/juce_linux_Midi.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/JUCE/modules/juce_audio_devices/native/juce_linux_Midi.cpp b/JUCE/modules/juce_audio_devices/native/juce_linux_Midi.cpp
index 74bbe4131..e70064848 100644
--- a/JUCE/modules/juce_audio_devices/native/juce_linux_Midi.cpp
+++ b/JUCE/modules/juce_audio_devices/native/juce_linux_Midi.cpp
@@ -387,7 +387,7 @@ static AlsaClient::Port* iterateMidiClient (const AlsaClient::Ptr& client,
     {
         if (snd_seq_query_next_port (seqHandle, portInfo) == 0
             && (snd_seq_port_info_get_capability (portInfo)
-                & (forInput ? SND_SEQ_PORT_CAP_SUBS_WRITE : SND_SEQ_PORT_CAP_SUBS_READ)) != 0)
+                & (forInput ? SND_SEQ_PORT_CAP_SUBS_READ : SND_SEQ_PORT_CAP_SUBS_WRITE)) != 0)
         {
             const String portName = snd_seq_port_info_get_name(portInfo);
 
