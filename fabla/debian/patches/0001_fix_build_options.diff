diff --git a/CMakeLists.txt b/CMakeLists.txt
index 436362e..a57b891 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -32,8 +32,8 @@ SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -Wall -O3 -Wno-unused-variable -fvisi
 
 IF(BUILD_SSE)
   IF(NOT ${CMAKE_SYSTEM_PROCESSOR} MATCHES "arm")
-    SET(CMAKE_C_FLAGS   "${CMAKE_C_FLAGS} -msse2 -mfpmath=sse -ffast-math")
-    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -msse2 -mfpmath=sse -ffast-math -Wno-trigraphs")
+    SET(CMAKE_C_FLAGS   "${CMAKE_C_FLAGS} -ffast-math")
+    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -ffast-math -Wno-trigraphs")
   ENDIF()
 ENDIF()
 
