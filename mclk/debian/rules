#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# output every command that modifies files on the build system.
#export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_install:
	dh_auto_install
	cp -R modgui $(shell pwd)/debian/zynthbox-plugin-mclk/zynthian/zynthian-plugins/lv2/mclk.lv2
	cp manifest.ttl $(shell pwd)/debian/zynthbox-plugin-mclk/zynthian/zynthian-plugins/lv2/mclk.lv2/

override_dh_usrlocal:
