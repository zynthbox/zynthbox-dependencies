diff --git a/Makefile b/Makefile
index dfbc330..f48de45 100644
--- a/Makefile
+++ b/Makefile
@@ -29,4 +29,7 @@ test:
 	cd test && make
 
 clean:
-	rm $(OBJECTS) $(EXE) && cd test && make clean
+	rm -rf $(OBJECTS) $(EXE) && cd test && make clean
+
+install: all
+	install -D $(EXE) $(DESTDIR)/usr/local/$(EXE)
